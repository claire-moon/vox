<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
# v0.0.5 branch and history audit

Snapshot: 2026-08-17, after `git fetch --prune --tags origin`.

This is an evidence and disposition record, not permission to delete anything.
The repository is intentionally keeping historical work until a final v0.0.5
release has been published and every candidate ref has been checked against
the reviewed successor chain.

## Protected release path

| Ref or item | Audit state | Required disposition |
|---|---|---|
| `main` at `b4a3e620ddc1df7ddcdaefca3f2e6d9cafd5e0f5` | Protected, current integration base | Keep. All release work reaches it only through reviewed checks. |
| `fix/v005-stability-foundation` / PR #9 | Blood-baseline restoration; remote head was `0fcfd03703de791eb792fbc7a777c5a657d485a1` and the local CI wording fix follows it | Merge after review and all hosted checks pass; do not merge merely to clear the branch. |
| `fix/v005-release-foundation` | Version, package, CI, QA, and audit foundation | Rebase onto the merged PR #9 head, review as the second serial PR, then merge normally. |
| `archive/v0.0.4-phase2-stability` at `114a93362dbb68693329526339a81f95662a83ce` | Annotated local archival tag for the pre-squash phase-2 head | Push unchanged once authenticated. It is not a release tag and must remain immutable. |

## Historic implementation refs

The following local refs are candidates for later archival/removal, not for
integration by wholesale merge:

`feat/voxel-materials`, `feat/chunked-terrain`, `feat/voxel-physics`,
`feat/digs-player-physics`, `feat/digs-terrain-tools`,
`feat/digs-steam-controls`, `feat/digs-headless`, `feat/software-renderer`,
and `feat/v004-phase0-gates`.

Their accepted work is represented by reviewed successor history and the
squash-integrated current `main` rather than necessarily by graph ancestry.
Before any ref is removed, compare its commit range or patch-id set with the
accepted chain, record a final disposition in the release evidence, and retain
the archival tag. `feat/digs-steam-controls` also has a remote counterpart;
do not prune it independently of that audit.

`git fsck --no-reflogs --unreachable` reported remaining unreachable commits
at this snapshot. They are not cleanup trash. Do not run `git gc`, expire
reflogs, or use destructive history rewriting before each remaining chain has
an explicit retention decision.

## Android isolation

| Ref / PR | State | Disposition |
|---|---|---|
| `port/android-v0.0.3` / PR #7 | Legacy Android port, conflicted with `main` | Do not merge into v0.0.5. Archive the PR and branch only after the final v0.0.5 publication. |
| `port/android-v0.0.3-ci-base` / PR #8 | Android CI-artifact base | Do not merge into v0.0.5. Archive with #7 after publication so its review and artifact provenance remain visible. |

No APK, Android SDK output, or mobile credentials belong in this repository's
release packages.

## Final cleanup order

1. Merge PR #9 and the release-foundation PR through protected `main`.
2. Complete the full v0.0.5 checklist and publish the tagged Linux, Windows,
   source, checksum, and SBOM assets.
3. Verify the public release and tag resolve to the accepted `main` commit.
4. Push the archival tag if it is not already remote, then archive PRs #7 and
   #8 and prune only refs with a recorded audit decision.
5. Remove only exact generated directories (`build`, `build-*`, `target`, and
   `dist`) after evidence collection; preserve runtime, QA evidence, lab media,
   and any user-owned worktree material.
