<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
# Pre-v0.0.5 branch audit

Snapshot: 2026-08-19. This is a retention and integration record, not a
release claim or permission to delete history.

## Current integration path

| Ref or item | Disposition |
|---|---|
| `main` / `origin/main` at `b4a3e62` | Protected base. Reach it only through reviewed PRs. |
| `fix/fixture-particle-stability` at `c012442` | Tested sandbox baseline. Merge before any release-tooling roll-up. |
| `fix/v005-safe-rollup` | Presentation and release-tooling follow-up. It must not change authoritative gameplay. |
| `archive/v0.0.4-phase2-stability` | Retain unchanged. Its final tree matches `main`, so it needs no code merge. |

## Superseded implementation work

`fix/v005-stability-foundation` and `fix/v005-release-foundation` remain
valuable evidence, but must not be merged wholesale. Their physics, gore,
blood, fixture, debris, and dropship changes predate the accepted
fixture/particle repair. Reuse only reviewed, non-simulation infrastructure.

The unreachable merge `69da2ce` supplied the approved death-message wording:
`FIRED` for player-caused deaths and `CLOCKED OUT` for environmental deaths.
Only that presentation copy is recovered; its gameplay changes remain out.

Historic `feat/voxel-materials`, `feat/chunked-terrain`,
`feat/voxel-physics`, `feat/digs-player-physics`,
`feat/digs-terrain-tools`, `feat/digs-steam-controls`, `feat/digs-headless`,
`feat/software-renderer`, and `feat/v004-phase0-gates` are audit records, not
merge candidates. Preserve them until the successor chain is fully reviewed.

## Android isolation

`port/android-v0.0.3` and `port/android-v0.0.3-ci-base` are deliberately out
of scope. Do not merge, rebase, close, delete, or update their workflows while
desktop v0.0.5 work is being consolidated.

## Cleanup boundary

Do not run history-rewriting or pruning commands, expire reflogs, or run an
aggressive garbage collection. Close superseded desktop review requests only
after the replacement has merged through protected `main`; retain the refs and
this audit record. The future `feat/v005-roadmap` branch begins only from that
reviewed `origin/main` commit.
